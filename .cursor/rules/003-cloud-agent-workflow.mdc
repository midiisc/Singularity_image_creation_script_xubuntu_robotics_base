---
name: Cloud Agent Workflow Rules
description: Specific rules for Cursor Cloud Agents and Background Agents - Branch coordination and approval workflow
version: "1.0"
alwaysApply: true
globs:
  - "**/*"
---

# 🚨 MANDATORY - CLOUD AGENT SPECIFIC RULES

## ⚠️ CRITICAL NOTICE FOR ALL CLOUD AGENTS

**THIS FILE APPLIES TO ALL CURSOR CLOUD AGENTS AND BACKGROUND AGENTS.**

**These rules are MANDATORY for agents running in cloud environments, isolated sessions, or background tasks.**

**VIOLATION OF THESE RULES IS STRICTLY PROHIBITED.**

## 🔴 CLOUD AGENT IDENTIFICATION

**You are a cloud agent if:**
- You are running in "Agent Mode"
- You are running in "Background Agent" mode
- You are running in an isolated cloud environment
- You are running autonomously without direct user interaction in the IDE

**If any of the above apply, you MUST follow these rules.**

## 🚨 CRITICAL: BRANCH COORDINATION WORKFLOW

### Mandatory Pre-Work Steps - NO EXCEPTIONS

**BEFORE performing ANY action, you MUST execute these commands in this exact order:**

```bash
# 1. Check current branch
git branch --show-current

# 2. Confirm the branch matches user instructions
git checkout <branch>   # only if a switch is required

# 3. Pull latest changes for the approved branch
git pull origin <branch>

# 4. Verify workspace state
git status
```

**DO NOT SKIP THESE STEPS. THEY ARE MANDATORY FOR ALL CLOUD AGENTS.**

### Initial Response Reporting Requirements

- ✅ **First response after initialization must explicitly report**:
  - The repository path you pulled from (e.g., `/workspace`)
  - The branch that was pulled (e.g., `beta`)
  - The commit message associated with the most recent `git pull`
- ✅ Only after providing this information may you proceed with additional context or actions in that response.

### Branch Rules

- ✅ **WORK ONLY ON USER-APPROVED BRANCHES**
- ❌ **FORBIDDEN**: Creating or switching to new branches without explicit approval
- ❌ **FORBIDDEN**: Creating `cursor/*` branches unless the user directs you
- ❌ **FORBIDDEN**: Creating pull requests without explicit approval
- ❌ **FORBIDDEN**: Pushing to any remote branch without explicit approval

### Pull Request Restrictions

**CLOUD AGENTS MUST NOT CREATE PULL REQUESTS WITHOUT EXPLICIT USER APPROVAL.**

- ❌ **NEVER run**: `gh pr create` unless instructed
- ❌ **NEVER use**: GitHub PR APIs without approval
- ❌ **NEVER create**: Pull requests through any mechanism unless the user asks for one
- ✅ **ONLY ALLOWED**: Direct commits to the approved branch (after user approval)
- ✅ **If the user explicitly authorizes a pull request**, you must post the PR URL, target branch, source branch, and PR title in chat immediately after creation and await confirmation that the PR was successfully created before continuing with further actions.

**Why:** Uncoordinated pull requests make it difficult to manage cloud-agent workflows. Always defer to user instructions.

## 🔒 Git Operations - Explicit User Approval Required

### Pulling Changes

**Allowed without explicit approval (but MANDATORY before work):**
```bash
git pull origin <branch>
```

**You MUST pull the latest changes for the approved branch before starting any work.**

### Pushing Changes

**NEVER push without explicit user approval.**

**Forbidden commands (without approval):**
- ❌ `git push`
- ❌ `git push origin <branch>`
- ❌ `git push -u origin <branch>`
- ❌ `git push --force`

**Required approval phrases from user:**
- "push to <branch>"
- "push changes"
- "push this"
- "commit and push"
- Any explicit instruction to push

**ONLY after hearing one of these phrases, you may:**
```bash
git push origin <branch>
```

### Committing Changes

**NEVER commit without explicit user approval.**

**Forbidden commands (without approval):**
- ❌ `git add`
- ❌ `git commit`
- ❌ `git commit -m`

**Required approval phrases from user:**
- "commit this"
- "commit changes"
- "save changes"
- "add and commit"

**ONLY after hearing one of these phrases, you may:**
```bash
git add -A
git commit -m "fix: Description of changes"
```

### Branch Operations

**NEVER create or switch branches without user approval.**

**Forbidden commands (without approval):**
- ❌ `git checkout -b <new-branch>`
- ❌ `git branch <new-branch>`
- ❌ `git switch -c <new-branch>`

**Allowed with approval:**
- ✅ `git checkout <branch>` (to move to the approved branch)
- ✅ `git branch --show-current` (to check current branch)

## 📋 Cloud Agent Workflow - Step by Step

### When Starting Work

1. **Check environment**:
   ```bash
   git branch --show-current
   ```
   - If output is not the approved branch → STOP and coordinate with the user

2. **Switch to the approved branch** (only with approval):
   ```bash
   git checkout <branch>
   ```

3. **Pull latest changes**:
   ```bash
   git pull origin <branch>
   ```

4. **Verify branch and status**:
   ```bash
   git status
   ```
   - Output MUST show you are on the approved branch with expected status

5. **NOW you can start work**

### When Completing Work

1. **Check what changed**:
   ```bash
   git status
   git diff
   ```

2. **Verify you're still on the approved branch**:
   ```bash
   git branch --show-current
   ```
   - If not correct → STOP and ask user

3. **Present changes to user**:
   - Show summary of changes
   - List modified files, including line ranges touched and a short purpose statement for each file
   - Ask: "Would you like me to commit these changes to <branch>?"

4. **WAIT for explicit approval**

5. **ONLY after approval, commit**:
   ```bash
   git add -A
   git commit -m "fix: <description>"
   ```

6. **Ask about pushing**:
   - "Changes committed to <branch>. Would you like me to push to origin/<branch>?"

7. **WAIT for explicit approval**

8. **ONLY after approval, push**:
   ```bash
   git push origin <branch>
   ```

## 🔁 Parallel Feature Branch Lifecycle (Cloud Agent)

When the user authorizes parallel work using feature branches:

1. **Create the feature branch from the approved base**
   - `git checkout <base-branch>`
   - `git pull origin <base-branch>`
   - `git checkout -b feature/<scope>`
   - Announce branch name, scope, and intended files in chat
2. **Implement changes**
   - Follow core-file restrictions unless the user expands scope
   - Keep work isolated to the feature branch
3. **Stage, commit, and push only after approval**
   - `git add -A`
   - `git commit -m "<type>: <summary>"`
   - `git push origin feature/<scope>`
4. **Run the merge feasibility check against beta**
   - `git fetch origin`
   - `git merge --no-commit --no-ff origin/beta`
   - Report the outcome (clean vs. conflicts + files) to the user
   - Run `git merge --abort` to reset the branch even if no conflicts occurred
5. **Request merge approval**
   - Provide diff summary, test status, and feasibility result
6. **Merge into beta when approved**
   - `git checkout beta`
   - `git pull origin beta`
   - `git merge --no-ff feature/<scope>`
   - Resolve conflicts with user guidance; commit and push to beta
7. **Clean up the feature branch**
   - `git branch -d feature/<scope>`
   - `git push origin --delete feature/<scope>`
8. **Main branch promotions**
   - Only execute when the user explicitly requests it; repeat feasibility check and cleanup for main

## 🚨 Cursor/* Branch Handling

**Cursor IDE sometimes auto-creates `cursor/*` branches for cloud agents.**

**If you find yourself on a `cursor/*` branch:**

1. **STOP immediately**
2. **Report to user**:
   ```
   ⚠️ WARNING: Currently on cursor/<branch-name>
    This branch was not approved for this task.
   ```

3. **Ask user for permission**:
   ```
    I need to switch to the approved branch. May I proceed?
   Commands I will run:
    1. git checkout <branch>
    2. git pull origin <branch>
   ```

4. **WAIT for explicit approval**

5. **ONLY after approval, switch to the approved branch**:
   ```bash
   git checkout <branch>
   git pull origin <branch>
   ```

6. **Continue work on the approved branch**

## 🔴 Multi-Agent Coordination

- ✅ **Assign distinct feature branches per agent when parallel work is approved**
- ✅ **All agents confirm the correct branch with the user**
- ✅ **All agents pull latest changes before starting work**
- ✅ **All agents coordinate pushes to avoid conflicts**
- ❌ **No pull requests** without user approval
- ❌ **No unsanctioned branches**

**Conflict Resolution:**
- If push fails due to remote changes:
  ```bash
  git pull --rebase origin <branch>
  git push origin <branch>
  ```
  - Only after user approval

## 📋 Cloud Agent Checklist

**Before starting ANY work:**
- [ ] I am on the user-approved branch (`git branch --show-current`)
- [ ] I have pulled the latest changes for that branch (`git pull origin <branch>`)
- [ ] I have confirmed whether a dedicated feature branch is expected
- [ ] I understand I MUST NOT create branches without approval
- [ ] I understand I MUST NOT create pull requests without approval
- [ ] I understand I MUST NOT push without explicit approval
- [ ] I understand I MUST NOT commit without explicit approval

**Before finishing ANY work:**
- [ ] I am still on the approved branch
- [ ] I have asked user for commit approval
- [ ] I have asked user for push approval
- [ ] I have run and reported the beta merge feasibility check if the branch targets beta
- [ ] I have NOT created any unapproved branches
- [ ] I have NOT created any pull requests

## 🔗 Related Rules

**This file works together with:**
- `.cursor/rules/001-agent-behavior.mdc` - Agent behavior and file creation rules
- `.cursor/rules/002-repository-workflow.mdc` - General repository workflow rules

**Cloud agents must follow ALL rules across all three files.**

## 💡 Summary for Cloud Agents

**Simple rules to remember:**
1. ✅ **Always confirm the correct branch with the user**
2. ✅ **Always sync the approved branch before starting**
3. ✅ **Always ask before committing**
4. ✅ **Always ask before pushing**
5. ✅ **Only push to branches the user approves**
6. ❌ **Never create unapproved branches**
7. ❌ **Never create pull requests without approval**
8. ❌ **Never continue on unexpected `cursor/*` branches without the user's consent**

**When in doubt: STOP, ASK USER, WAIT FOR APPROVAL.**

---

**THESE RULES ARE MANDATORY FOR ALL CURSOR CLOUD AGENTS.**
**VIOLATIONS WILL BE DETECTED AND BLOCKED.**
