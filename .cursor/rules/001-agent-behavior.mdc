---
name: Agent Behavior Rules
description: Core AI agent behavior and file creation protocols - SOURCE OF TRUTH for behavior enforcement
version: "1.0"
alwaysApply: true
globs:
  - "**/*"
---

# 🚨 MANDATORY - READ FIRST - AI AGENT BEHAVIOR RULES

## ⚠️ CRITICAL ENFORCEMENT NOTICE

**YOU MUST READ AND STRICTLY ADHERE TO ALL RULES IN THIS FILE.**

**This file is AUTOMATICALLY ENFORCED by Cursor IDE. These rules are MANDATORY and NON-NEGOTIABLE.**

**VIOLATION OF THESE RULES IS STRICTLY PROHIBITED. You MUST follow these rules without exception.**

## 🔴 BEFORE STARTING ANY WORK - MANDATORY VERIFICATION

**YOU MUST complete this checklist BEFORE performing ANY action:**

- [ ] **I have read and understand ALL rules in this file**
- [ ] **I understand that file creation is RESTRICTED** (see FILE CREATION RESTRICTIONS below)
- [ ] **I understand that I will only present a plan when the user explicitly asks or when I need clarification, and any plan must be inline**
- [ ] **I understand that code reviews must be CHUNKED and INLINE**
- [ ] **I will NOT create any documentation/summary/plan files without explicit user request**
- [ ] **I will respond INLINE in chat unless explicitly asked to create a file**

**IF YOU ARE UNSURE ABOUT ANY ACTION, YOU MUST ASK THE USER FIRST.**

## ⚠️ SOURCE OF TRUTH - AI Agent Behavior Rules

**This file is the single source of truth for all AI agent behavior rules.**  
**All other files reference this file. To update rules, edit this file only.**

## 🚨 CRITICAL PROTOCOLS - NEVER VIOLATE - STRICTLY ENFORCED

### FILE CREATION RESTRICTIONS - ABSOLUTELY MANDATORY

**YOU MUST NOT create any non-functional files** (summaries, plans, documentation, analysis reports) unless the user explicitly requests file creation using phrases like:
- "create a file"
- "write a summary file"
- "generate steps file"
- "save this to a file"
- "export to file"
- "create documentation"

**FORBIDDEN FILE TYPES** - Never create these automatically:
- `summary.md`, `SUMMARY.txt`, `overview.*`
- `steps.md`, `plan.md`, `roadmap.md`, `TODO.md`
- `notes.md`, `documentation.md`, `guide.md`
- `analysis.md`, `report.md`, `findings.md`
- `review.md`, `code-review.md`
- Any `.txt` or `.md` file not explicitly requested

**ALLOWED without explicit request**:
- Source code files (`.sh`, `.py`, `.cpp`, `.ts`, `.js`, etc.) when implementing features
- Configuration files when setting up tools/frameworks
- Test files when asked to write tests
- Modifications to existing files when requested

### RESPONSE MODE

**Always respond inline in the chat**:
- Provide explanations, code snippets, or solutions directly in the conversation
- Never write responses to new files automatically
- Use code references for existing code, markdown code blocks for new code

### EXECUTION-FIRST APPROACH

- Default to working directly on the requested code or task without presenting a plan.
- Only provide a plan when the user explicitly requests one or when you are genuinely uncertain and need confirmation before proceeding.
- When a plan is necessary, present it inline in chat (never as a file) and clearly state why the plan is being shown.
- Do NOT create separate plan files unless the user explicitly instructs you to do so.

### CODE REVIEW PROTOCOL

**Chunked Review Approach**:
1. **For large code reviews**:
   - Break the review into logical chunks (by file, by feature, by module)
   - Review one chunk at a time
   - Present findings inline in chat
   - Wait for user feedback before moving to next chunk

2. **Review Format**:
   - Use inline code references for existing code
   - Provide specific line numbers and file paths
   - Focus on actionable feedback
   - Prioritize critical issues first

3. **Never create separate review files**:
   - All review feedback should be in chat
   - Only create review files if explicitly requested

### WORKFLOW PRINCIPLES

**Efficiency First**:
- Execute directly when the task is clear and straightforward
- Ask for clarification only when genuinely uncertain
- Avoid unnecessary intermediate steps
- Focus on delivering working solutions

**Code Changes**:
- Modify existing files when requested (this is allowed and expected)
- Create new source/implementation files when part of development tasks
- Documentation/summary/plan files require explicit user confirmation
- When presenting the final response after making edits, explicitly list each modified file along with the line ranges touched and a short explanation of the purpose of those changes.

**Git Operation Communication**:
- Immediately after executing any git-related command (branch checkouts, pulls, commits, pushes, merges, rebases, etc.), send a clear message in chat summarizing the command, why it was run, and the outcome.
- Include any follow-up actions that will happen automatically (such as merge workflows) so the user always knows the current repository state.

### UNCERTAINTY PROTOCOL

**If Uncertain**:
1. Explain the uncertainty inline in chat and request the specific clarification needed.
2. Outline a brief approach or plan only if it is required for the clarification.
3. Ask: "Would you like me to proceed with this approach?" or "Should I create a file for this?"
4. Wait for explicit confirmation before proceeding and do NOT create files "just in case."

### EXAMPLES

❌ **BAD - Creating files automatically**:
- User: "Summarize the codebase"
- AI: *Creates summary.md automatically* ← FORBIDDEN

✅ **GOOD - Responding inline**:
- User: "Summarize the codebase"
- AI: *Provides summary in chat, no file created*

✅ **GOOD - Explicit request**:
- User: "Create a summary.md file with the codebase overview"
- AI: *Creates the file as requested*

❌ **BAD - Creating plan files**:
- User: "Implement feature X"
- AI: *Creates plan.md automatically* ← FORBIDDEN

✅ **GOOD - Planning inline**:
- User: "Implement feature X"
- AI: *Presents plan in chat, waits for confirmation, then executes*

✅ **GOOD - Chunked review**:
- User: "Review this large codebase"
- AI: *Reviews File1.sh, presents findings inline, asks if ready for File2.sh*

### MANDATORY ENFORCEMENT - BEFORE ANY FILE CREATION

**YOU MUST validate before creating ANY file:**

1. **Is this a source/config/test file for implementation?** 
   - → If YES: Allowed (but verify with user if unsure)
   - → If NO: Continue to step 2

2. **Is this a documentation/summary/plan file?**
   - → If YES: **REQUIRE EXPLICIT USER REQUEST** - Do NOT create without explicit phrase
   - → If NO: Continue to step 3

3. **When in doubt:**
   - → **STOP and ASK USER FIRST**
   - → Present approach inline in chat
   - → Wait for explicit confirmation
   - → **DO NOT create files "just in case"**

**VALID USER REQUEST PHRASES (must contain one of these):**
- "create file named X"
- "write this to a file"
- "save as filename.ext"
- "export to file"
- "generate documentation file"
- "create a [filename] file"
- "save this as a file"

**IF USER DOES NOT USE ONE OF THESE PHRASES, DO NOT CREATE THE FILE.**

## 🔴 VIOLATION CONSEQUENCES

**If you violate these rules:**
1. **STOP immediately**
2. **Explain the violation**
3. **Ask user for explicit permission**
4. **Wait for confirmation before proceeding**

**THESE RULES ARE AUTOMATICALLY ENFORCED. VIOLATIONS WILL BE DETECTED.**

## REMEMBER: INLINE FIRST, FILES ONLY WHEN EXPLICITLY REQUESTED - STRICTLY ENFORCED

## 🔗 Related Rules

**Workflow Rules**: See `.cursor/rules/002-repository-workflow.mdc` for:
- Git workflow and branch management
- File editing restrictions
- Commit and push procedures
- Repository maintenance principles

**These rules work together**: Behavior rules (this file) govern AI responses and file creation, while workflow rules govern git operations and repository management.
