**CODE CHECK PROMPT MANUAL - MASTER ENTRY POINT**

**AUTHORITATIVE SOURCE**: This file (`Code_check_prompt_manual.txt`) is the **master entry point** for the comprehensive code checking manual in this repository. It orchestrates sequential checking across all parts while maintaining 500-line context limits.

**🚨 MANDATORY PRE-REVIEW TASK COMPLETION CHECK (BEFORE STARTING REVIEW)**:
- **BEFORE LOADING PART 1**: You MUST check for any earlier pending tasks from previous sessions, conversations, or incomplete work.
- **PENDING TASK HANDLING**: 
  - If any pending tasks are found, you MUST either:
    1. Complete them immediately before starting the review, OR
    2. Add them to the comprehensive todo list that will be created for this review (see "COMPREHENSIVE TODO LIST REQUIRED" section)
  - Do NOT start the review process until all earlier pending tasks are either completed or integrated into the current todo list.
- **TASK SOURCES TO CHECK**:
  - Previous conversation history and incomplete todos
  - Any uncommitted changes that need review or completion
  - Any pending file modifications or corrections
  - Any incomplete validation or testing tasks
  - Any pending documentation or cleanup tasks
- **VERIFICATION REQUIRED**: Before proceeding to "MANDATORY EXECUTION PROTOCOL", verify that:
  - All earlier pending tasks are identified
  - All identified tasks are either completed or added to the comprehensive todo list
  - No tasks are left unaccounted for
- **ONLY AFTER VERIFICATION**: Proceed to create the comprehensive todo list and begin PART 1 review.

**SEQUENTIAL PROCESSING ARCHITECTURE**:
This manual is split into 4 parts (each under 500 lines) to maintain full context throughout the review process. When this file is referenced, ALL parts MUST be processed sequentially:

1. **PART 1**: `Code_check_prompt_manual-PART1.txt` (Introduction, Steps 1-2, Sections A-B-C-D partial)
2. **PART 2**: `Code_check_prompt_manual-PART2.txt` (continues D, covers E-F-G-H)
3. **PART 3**: `Code_check_prompt_manual-PART3.txt` (continues H, covers I-J-K-L-M-O partial)
4. **PART 4**: `Code_check_prompt_manual-PART4.txt` (continues O, covers P, Steps 3-4, Protocols)

**MANDATORY EXECUTION PROTOCOL FOR AI AGENTS**:
When this master entry point is referenced, you MUST:

0. **PRE-REVIEW TASK CHECK (MANDATORY FIRST STEP)**: Before starting the review, check for any earlier pending tasks (see "🚨 MANDATORY PRE-REVIEW TASK COMPLETION CHECK" section above). Complete them or add them to the comprehensive todo list. Only proceed after all earlier pending tasks are accounted for.
1. **LOAD PART 1 FIRST**: Read `prompts/Code_check_prompt_manual-PART1.txt` completely
2. **PROCESS ALL CHECKLIST ITEMS IN PART 1**: Execute all checklist items (A1-A6, B1-B3, C1-C6, D1-D4 partial) from PART 1
3. **ONLY AFTER PART 1 COMPLETE**: Load `prompts/Code_check_prompt_manual-PART2.txt`
4. **PROCESS ALL CHECKLIST ITEMS IN PART 2**: Execute all checklist items (continues D, E1-E9, F1-F3, G1-G5, H1-H4 partial) from PART 2
5. **ONLY AFTER PART 2 COMPLETE**: Load `prompts/Code_check_prompt_manual-PART3.txt`
6. **PROCESS ALL CHECKLIST ITEMS IN PART 3**: Execute all checklist items (continues H, I1-I4, J1-J3, K1-K1b-K3, L1-L6, M1-M14) from PART 3
7. **ONLY AFTER PART 3 COMPLETE**: Load `prompts/Code_check_prompt_manual-PART4.txt`
8. **PROCESS ALL CHECKLIST ITEMS IN PART 4**: Execute all checklist items (N1-N5, O1-O5, P1-P5, Steps 3-4, Protocols) from PART 4
9. **FINAL SHELLCHECK VALIDATION AFTER ALL PARTS**: After ALL parts (PART 1-4) are complete and all corrections applied, run final ShellCheck validation on the ENTIRE corrected script. Fix all ShellCheck errors and warnings. Re-run ShellCheck until zero errors and zero warnings.
10. **POST-REVIEW TODO COMPLETION CHECK (MANDATORY)**: Perform final comprehensive check of ALL todos (see "🚨 MANDATORY POST-REVIEW TODO COMPLETION CHECK" section below). Complete any incomplete todos. Only proceed after ALL todos are marked as completed.
11. **FINAL CONFIRMATION**: Verify all checklist items A1 through P5 have been checked across all parts AND final ShellCheck validation passes with zero errors/warnings AND all todos are completed

**🚨 CRITICAL: CONTINUOUS EXECUTION WITHOUT BREAKS (MANDATORY)**:
- **ALL 4 PARTS MUST BE COMPLETED IN ONE CONTINUOUS STRETCH**: The entire review process (PART 1 → PART 2 → PART 3 → PART 4 → ShellCheck → Final Confirmation) MUST be executed without any breaks, pauses, or interruptions.
- **NO MANUAL CONFIRMATION REQUESTS**: Do NOT ask for user confirmation, approval, or permission at any point during the review process. Execute all parts autonomously and continuously.
- **NO PARTIAL COMPLETION**: Do NOT stop after completing PART 1, PART 2, or PART 3. Continue immediately to the next part without any delay or confirmation request.
- **NO PENDING TODOS**: All todos MUST be completed fully before stopping. Do NOT leave any todos pending for later confirmation or steps. Every single todo item must be marked as completed before the review process ends.
- **COMPREHENSIVE TODO LIST REQUIRED**: Before starting PART 1, create a comprehensive todo list that includes:
  - Every single checklist item from PART 1 (A1-A6, B1-B3, C1-C6, D1-D4 partial)
  - Every single checklist item from PART 2 (continues D, E1-E9, F1-F3, G1-G5, H1-H4 partial)
  - Every single checklist item from PART 3 (continues H, I1-I4, J1-J3, K1-K1b-K3, L1-L6, M1-M14)
  - Every single checklist item from PART 4 (N1-N5, O1-O5, P1-P5, Steps 3-4, Protocols)
  - ShellCheck validation task
  - Spellcheck validation task (if applicable)
  - Final confirmation task
- **TODO EXECUTION PROTOCOL**: Execute todos strictly in order, one at a time, marking each as completed before starting the next. Continue until ALL todos are completed. Do NOT stop until every single todo is marked as completed.
- **ZERO TOLERANCE FOR INCOMPLETE WORK**: If any todo remains pending or incomplete, the review process is considered incomplete. Continue working until ALL todos are fully completed.

**HARD ENFORCEMENT – SINGLE-PART MEMORY PROTOCOL**:
- Exactly one part (PART 1, 2, 3, or 4) may be resident in working memory at any time.
- Before loading a part, explicitly unload all prior part content from memory.
- Retain only a compact state capsule containing:
  - Minimal pass/fail/N/A map of completed checklist items,
  - A symbol/context ledger (names only, no code) necessary for continuity,
  - Active chunk cursor (chunk index and line range).
- After completing a part for all chunks, purge that part's content from memory before loading the next part.
- Do not interleave or co-load multiple parts.

**HARD ENFORCEMENT – CODE CHUNKING (MAX ~500 LINES)**:
- Before any checklist processing, split the input code into chunks of max 500 lines (target 450–500), with 20–40 lines of overlap between adjacent chunks.
- Preserve natural boundaries where feasible (functions/guards/loops/case). If boundaries would exceed 500 lines, honor the size limit and rely on overlap for context.
- Process chunks in order. For each chunk:
  1) Run the currently loaded part's checklist across the entire chunk,
  2) Apply all necessary corrections in-place,
  3) Re-run the same part's checks on the corrected chunk until PASS/N/A,
  4) Run ShellCheck validation on the corrected chunk (after all checklist corrections),
  5) Fix any ShellCheck issues found,
  6) Re-run ShellCheck until zero errors/warnings,
  7) Update the compact state capsule (no large code stored).
- Only after all chunks pass for the current part AND all chunks are ShellCheck compliant may you unload it and proceed to the next part.

**CHUNK LOADING/UNLOADING PROTOCOL (MANDATORY)**:
- Exactly one code chunk may be resident in working memory at any time.
- On chunk load:
  1) Purge all non-capsule memory from any previously processed chunk(s),
  2) Rehydrate context strictly from the compact state capsule plus the newly loaded chunk's text,
  3) Ensure the active range is limited to the current chunk lines, including only the designated 20–40 line overlap.
- During processing:
  - Do not reference code outside the current chunk window except the permitted overlap.
  - Do not cache embeddings, token buffers, or raw text from prior chunks beyond the capsule.
  - All cross-chunk reasoning must be mediated through the capsule entries (names/flags/cursors), never by retaining prior chunk code.
- On chunk unload (after PASS/N/A for the current part):
  1) Write back updated results to the capsule (checklist pass map, symbol/context ledger, next chunk cursor),
  2) Purge the current chunk’s text and any transient analysis artifacts,
  3) Proceed to load the next chunk and rehydrate only from the capsule.

**CHUNK MEMORY SAFEGUARDS (STRICT)**:
- Capsule must contain only minimal metadata: pass/fail maps, symbol names (not code), and cursor positions.
- No speculative multi-chunk loading, peeking, or pre-tokenization is allowed.
- No retention of prior chunk diffs, patches, or ASTs; only capsule metadata persists.
- Overlap discipline:
  - Overlap may be read and reasoned about, but duplication of non-overlap code across chunk boundaries is forbidden.
  - If edits affect overlap lines, ensure idempotent re-validation when the next chunk loads; do not carry raw text across the boundary.
- Determinism requirement:
  - Rehydration from the capsule must yield identical decisions independent of any hidden buffers.
  - Any discovery of hidden retained state is treated as a protocol violation; purge immediately and restart current chunk from capsule.

**PROHIBITIONS TO PREVENT MEMORY LEAKS/DRIFT**:
- No vector-store or embedding caches across chunks.
- No streaming accumulation of token histories beyond the current chunk.
- No background “global summary” growth beyond the capsule schema (pass map, symbol/context names, cursor).
- No parallel chunk co-loading; chunks must be processed strictly one-at-a-time.
- No cross-part shortcuts; all cross-part context must flow through the capsule, not retained code.

**BATCHED TODO GENERATION & EXECUTION (COMPREHENSIVE TODO LIST - MANDATORY)**:
- **INITIAL COMPREHENSIVE TODO LIST (MANDATORY)**: Before starting PART 1, create a COMPLETE comprehensive todo list that includes:
  - **ALL checklist items from PART 1**: A1-A6, B1-B3, C1-C6, D1-D4 (partial)
  - **ALL checklist items from PART 2**: continues D, E1-E9, F1-F3, G1-G5, H1-H4 (partial)
  - **ALL checklist items from PART 3**: continues H, I1-I4, J1-J3, K1-K1b-K3, L1-L6, M1-M14
  - **ALL checklist items from PART 4**: N1-N5, O1-O5, P1-P5, Steps 3-4, Protocols
  - **ShellCheck validation**: Final ShellCheck validation on entire script
  - **Spellcheck validation**: Spellcheck validation (if applicable)
  - **Final confirmation**: Verify all checklist items completed and ShellCheck passed
- **EXECUTION PROTOCOL**: Execute todos strictly in order, one at a time, marking each as completed before starting the next.
- **BATCHED EXECUTION (OPTIONAL FOR LARGE LISTS)**: For very large todo lists, you may process in batches of 20:
  1) Execute first 20 todos strictly in order (1→20), one at a time, no interleaving, no skipping,
  2) Mark each item completed before starting the next,
  3) Only after all 20 are completed, proceed to next 20 and continue,
  4) Repeat until ALL todos in the comprehensive list are completed.
- **MANDATORY COMPLETION**: Do NOT stop until ALL todos are marked as completed. Do NOT leave any todos pending for later confirmation or steps. Every single todo must be completed before the review process ends.
- **ZERO TOLERANCE**: If any todo remains pending or incomplete, the review process is considered incomplete. Continue working until ALL todos are fully completed.
- The batching protocol is mandatory and layered on top of single-part memory and 500-line chunking. Do not advance parts or chunks while a batch is incomplete.

ONE-PART-AT-A-TIME ENFORCEMENT:
- STRICT: Process exactly one part at a time. Do not interleave items from different parts.
- COMPLETE-IN-PART: For the current part, perform all checks AND apply all corrections discovered in that part immediately.
- RE-VALIDATE: After applying corrections for the current part, re-run that part's checks to confirm PASS/N/A before proceeding.
- SHELLCHECK-VALIDATE: After all part corrections are complete, run ShellCheck validation. Fix all ShellCheck issues. Re-run ShellCheck until zero errors/warnings. Only then proceed to next part.
- CARRY FORWARD: Persist outputs/context needed by the next part (findings, symbol tables, flags), then continue to the next part.
- NO SKIPPING: Do not load the next part until the current part is fully completed, re-validated, AND ShellCheck compliant.

**CONTEXT MANAGEMENT**:
- Each part is processed independently with full context (under 500 lines each)
- Symbol tables and cross-references maintained across parts (as documented in each part)
- No checklist items skipped - sequential processing ensures complete coverage
- Each part references previous parts for continuity

**USAGE FOR USERS**:
- **Single entry point**: Reference `prompts/Code_check_prompt_manual.txt` (this file)
- **Automatic sequential processing**: AI agents automatically load and process all parts in sequence
- **Full coverage guaranteed**: All checklist items A1-P5 are checked without missing any
- **Context limits maintained**: Each part stays under 500 lines for optimal context window usage

**FOR TOOL INTEGRATION**:
- **Python scripts**: Load this master file, then sequentially read PART1 → PART2 → PART3 → PART4
- **Shell scripts**: Reference this master file, tooling should handle sequential loading
- **Pre-commit hooks**: Load this master file, process all parts sequentially
- **CI/CD**: Reference this master file, validate across all parts sequentially

**MANDATORY FOR ALL REVIEWS**: 
- ALL other review prompts (Advanced CoT, Enhanced Code Review, etc.) MUST reference this master entry point
- This master file MUST be enhanced FIRST when new patterns are discovered from AI corrections, chats, or CI checks
- All checklist items A1 through P5 MUST be checked sequentially across all parts - do not skip any items
- Pattern learning updates MUST be added to the appropriate PART file FIRST before being referenced elsewhere

**NON-INTERACTIVE AUTONOMY (NO APPROVAL PROMPTS - MANDATORY CONTINUOUS EXECUTION)**:
- **MANDATORY**: Agents MUST operate fully autonomously under this manual: do not stop to ask the user for approval, confirmation, or to proceed between chunks or parts.
- **MANDATORY**: Provide brief informational status updates only; never block awaiting user input during PART1 → PART2 → PART3 → PART4 processing.
- **MANDATORY**: Continue applying fixes and re-validating per-part, per-chunk automatically until the entire file passes all parts.
- **MANDATORY**: Complete ALL 4 parts in one continuous stretch without any breaks, pauses, or interruptions.
- **MANDATORY**: Create a comprehensive todo list covering ALL checklist items from ALL 4 parts, ShellCheck, and spellcheck before starting. Complete ALL todos fully before stopping.
- **MANDATORY**: Do NOT leave any todos pending for later confirmation or steps. Every single todo must be marked as completed.
- **RARE EXCEPTION**: Only pause if multiple corrective strategies exist with materially different trade-offs; in that rare case, present options with concise pros/cons and wait. Otherwise, proceed without interruption.
- **PRECEDENCE**: If any other prompt or instruction conflicts with this autonomy policy, this section takes precedence for workflows governed by this manual.

**IMMEDIATE EXECUTION – NO PLAN-ONLY OUTPUT (COMPREHENSIVE TODO LIST REQUIRED)**:
- **FIRST STEP**: Create comprehensive todo list covering ALL checklist items from ALL 4 parts, ShellCheck, and spellcheck before starting execution.
- Begin execution immediately on PART 1, Chunk 1 (lines 1–~500) without emitting plan-only preambles.
- Do not output intent-only messaging (e.g., "I'll start", "Planning", "Preparing"). Replace with action-tied, declarative updates (e.g., "PART 1 on Chunk 1 executed: 0 fixes" or "Applied 3 fixes; re-validated PASS").
- After any "Starting" line, the next output must reflect completed action (read/audit/apply/re-validate) on a concrete chunk or part.
- Suppress redundant status. Only report: action taken → result → next chunk/part in progress.
- If a chunk requires no corrections, proceed immediately to the next chunk and state so in one line.
- **CONTINUOUS EXECUTION**: Continue executing todos from the comprehensive list until ALL are completed. Do NOT stop until every single todo is marked as completed.

**SHELLCHECK COMPLIANCE REQUIREMENTS (MANDATORY - FINAL VALIDATION ONLY)**:
- **INDUSTRY BEST PRACTICE**: ShellCheck is the industry-standard static analysis tool for shell scripts. All corrections MUST be ShellCheck compliant.
- **ZERO TOLERANCE POLICY**: Scripts MUST pass ShellCheck with zero errors and zero warnings before audit completion.
- **FINAL VALIDATION ONLY**: ShellCheck validation runs ONCE at the END of ALL parts (PART 1-4), AFTER all corrections are applied:
  - Process PART 1 → Apply all corrections → Continue to PART 2 (NO ShellCheck yet)
  - Process PART 2 → Apply all corrections → Continue to PART 3 (NO ShellCheck yet)
  - Process PART 3 → Apply all corrections → Continue to PART 4 (NO ShellCheck yet)
  - Process PART 4 → Apply all corrections → Run FINAL ShellCheck on ENTIRE script → Fix all issues → Re-validate → Complete audit
- **RATIONALE**: Running ShellCheck only at the end ensures all corrections are complete before validation, avoids redundant checks, and provides a single comprehensive validation pass
- **SHELLCHECK COMMAND OPTIONS**:
  - For standalone scripts: `shellcheck script.sh` or `shellcheck -s bash script.sh` (if Bash-specific)
  - For sourced scripts: `shellcheck -x script.sh` (allows external sourcing)
  - For POSIX compliance: `shellcheck -s sh script.sh` (strict POSIX mode)
  - Machine-readable output: `shellcheck --format=gcc script.sh` (for CI/CD integration)
- **AUTO-FIX WORKFLOW**:
  1. Run ShellCheck: `shellcheck script.sh` (or appropriate options)
  2. Capture all errors and warnings with error codes (e.g., SC2086, SC2155)
  3. Apply corrections for each ShellCheck issue
  4. Re-run ShellCheck to verify fixes
  5. Repeat until zero errors and zero warnings
  6. Document all fixes with ShellCheck error codes in corrections summary
- **COMMON SHELLCHECK ISSUES TO FIX**:
  - SC2086: Double quote to prevent globbing and word splitting
  - SC2155: Declare and assign separately to avoid masking return values
  - SC1090: Can't follow non-constant source (use `# shellcheck source=path` directive)
  - SC1091: Not following sourced file (use `# shellcheck source=path` directive)
  - SC2001: Use `${var//search/replace}` instead of `sed`
  - SC2004: `$`/`${}` is unnecessary on arithmetic variables
  - SC2006: Use `$(..)` instead of backticks
  - SC2015: Note that `A && B || C` is not `if-then-else`
  - SC2034: Variable appears unused (verify or remove)
  - SC2046: Quote this to prevent word splitting
  - SC2068: Double quote array expansions to avoid re-splitting elements
  - SC2086: Double quote to prevent globbing and word splitting
  - SC2119: Use `"$@"` if function's `$1` should mean script's `$1`
  - SC2120: Function doesn't end with `return` statement
  - SC2128: Expanding an array without an index only gives the first element
  - SC2145: Argument mixes string and array (use `"${array[@]}"` or `"${array[*]}"`)
  - SC2154: Variable is referenced but not assigned (check for typos)
  - SC2164: Use `cd ... || exit` or `cd ... || return` in case `cd` fails
  - SC2181: Check exit code directly with `if mycmd;`, not indirectly with `$?`
  - SC2207: Prefer `mapfile` or `read -a` to split command output (or quote to avoid splitting)
- **INTEGRATION WITH CORRECTIONS**:
  - All corrections applied during checklist processing (A1-P5) MUST be ShellCheck compliant
  - If a correction introduces a ShellCheck issue, fix it immediately
  - ShellCheck validation at part end ensures all corrections are compliant
  - Final ShellCheck validation ensures the entire script is compliant
- **DOCUMENTATION REQUIREMENT**: All ShellCheck fixes MUST be documented with error codes in the corrections summary (e.g., "SC2086: Fixed unquoted variable in line 42")

**FILE AND DIRECTORY HANDLING REQUIREMENTS (MANDATORY)**:
- **MISSING PARENT DIRECTORY HANDLING**: When writing to a file or creating a directory, if the parent directory does not exist:
  - **MANDATORY**: Show a warning message indicating the parent directory is missing
  - **MANDATORY**: Create the parent directory (and any intermediate directories) using `mkdir -p`
  - **MANDATORY**: Verify the directory was created successfully before proceeding
  - **EXAMPLE - CORRECT**:
    ```bash
    OUTPUT_FILE="/path/to/nonexistent/dir/file.txt"
    PARENT_DIR=$(dirname "${OUTPUT_FILE}")
    
    if [ ! -d "${PARENT_DIR}" ]; then
      echo "[WARNING] Parent directory does not exist: ${PARENT_DIR}"
      echo "[INFO] Creating parent directory: ${PARENT_DIR}"
      mkdir -p "${PARENT_DIR}" || {
        echo "[ERROR] Failed to create parent directory: ${PARENT_DIR}"
        return 1
      }
      echo "[INFO] Parent directory created successfully: ${PARENT_DIR}"
    fi
    
    # Now safe to write to file
    echo "content" > "${OUTPUT_FILE}"
    ```
  - **EXAMPLE - WRONG** (no parent directory check):
    ```bash
    # This will fail if /path/to/nonexistent/dir doesn't exist
    echo "content" > "/path/to/nonexistent/dir/file.txt"
    ```
- **MISSING FILE HANDLING**: When a file is expected to exist but doesn't:
  - **MANDATORY**: Show a warning message indicating the file is missing
  - **MANDATORY**: Create the file with appropriate permissions and content (if applicable)
  - **MANDATORY**: Verify the file was created successfully before proceeding
  - **EXAMPLE - CORRECT**:
    ```bash
    CONFIG_FILE="/etc/myapp/config.conf"
    
    if [ ! -f "${CONFIG_FILE}" ]; then
      echo "[WARNING] Configuration file does not exist: ${CONFIG_FILE}"
      echo "[INFO] Creating configuration file: ${CONFIG_FILE}"
      
      # Create parent directory if needed
      PARENT_DIR=$(dirname "${CONFIG_FILE}")
      if [ ! -d "${PARENT_DIR}" ]; then
        mkdir -p "${PARENT_DIR}" || {
          echo "[ERROR] Failed to create parent directory: ${PARENT_DIR}"
          return 1
        }
      fi
      
      # Create file with default content
      cat > "${CONFIG_FILE}" << 'EOF'
      # Default configuration
      key=value
      EOF
      
      # Verify file was created
      if [ ! -f "${CONFIG_FILE}" ]; then
        echo "[ERROR] Failed to create configuration file: ${CONFIG_FILE}"
        return 1
      fi
      
      echo "[INFO] Configuration file created successfully: ${CONFIG_FILE}"
    fi
    ```
  - **EXAMPLE - WRONG** (no file existence check):
    ```bash
    # This will fail if file doesn't exist
    source "/etc/myapp/config.conf"
    ```
- **INTEGRATION WITH ERROR HANDLING**: File and directory creation MUST be integrated with proper error handling:
  - Check exit codes of `mkdir -p` and file creation operations
  - Provide clear error messages if creation fails
  - Use appropriate return codes or exit codes on failure
  - Log warnings and actions taken for auditability
- **PERMISSIONS CONSIDERATION**: When creating directories or files:
  - Use appropriate permissions (consider `umask` and security requirements)
  - For sensitive files/directories, set restrictive permissions explicitly
  - Document permission choices in comments when non-standard
- **APPLICABILITY**: These requirements apply to:
  - File write operations (`>`, `>>`, `tee`, etc.)
  - Directory creation operations (`mkdir`, `install -d`, etc.)
  - Configuration file handling
  - Log file creation
  - Temporary file/directory creation (though `mktemp` handles this automatically)
  - Any operation that assumes a file or directory exists

**VERIFICATION**:
After processing all parts, confirm:
- ✅ PART 1 checklist items processed (A1-A6, B1-B3, C1-C6, D1-D4 partial)
- ✅ PART 2 checklist items processed (continues D, E1-E9, F1-F3, G1-G5, H1-H4 partial)
- ✅ PART 3 checklist items processed (continues H, I1-I4, J1-J3, K1-K1b-K3, L1-L6, M1-M14)
- ✅ PART 4 checklist items processed (N1-N5, O1-O5, P1-P5, Steps 3-4, Protocols)
- ✅ Final ShellCheck validation passed on ENTIRE script (zero errors, zero warnings) - runs ONCE after all parts complete
- ✅ All checklist items A1 through P5 checked sequentially without skipping any

**🚨 MANDATORY POST-REVIEW TODO COMPLETION CHECK (BEFORE FINALIZING)**:
- **AFTER ALL PARTS COMPLETE**: You MUST perform a final comprehensive check of ALL todos before finalizing the review.
- **TODO VERIFICATION REQUIRED**:
  - Review the comprehensive todo list created at the beginning
  - Verify that EVERY single todo item is marked as "completed"
  - Check for any todos that may have been marked as "pending", "in_progress", or "cancelled"
  - Identify any todos that were added during the review process but not yet completed
- **INCOMPLETE TODO HANDLING**:
  - If ANY todo remains incomplete, pending, or in progress:
    1. **STOP finalization immediately**
    2. **Complete ALL incomplete todos** before proceeding
    3. **Re-verify** that all todos are marked as completed
    4. **Only then** proceed to final confirmation
  - Do NOT finalize the review if any todos are incomplete
  - Do NOT leave any todos for "later" or "future" completion
- **FINAL TODO CHECKLIST**:
  - ✅ All PART 1 checklist items (A1-A6, B1-B3, C1-C6, D1-D4 partial) - todos completed
  - ✅ All PART 2 checklist items (continues D, E1-E9, F1-F3, G1-G5, H1-H4 partial) - todos completed
  - ✅ All PART 3 checklist items (continues H, I1-I4, J1-J3, K1-K1b-K3, L1-L6, M1-M14) - todos completed
  - ✅ All PART 4 checklist items (N1-N5, O1-O5, P1-P5, Steps 3-4, Protocols) - todos completed
  - ✅ ShellCheck validation task - todo completed
  - ✅ Spellcheck validation task (if applicable) - todo completed
  - ✅ Final confirmation task - todo completed
  - ✅ Any earlier pending tasks (from pre-review check) - todos completed
  - ✅ Any tasks added during review process - todos completed
- **ZERO TOLERANCE**: If even ONE todo is incomplete, the review is NOT complete. Continue working until ALL todos are fully completed.
- **ONLY AFTER ALL TODOS COMPLETE**: Proceed to final confirmation and mark the review as complete.

---

**PART FILES REFERENCE**:
- `prompts/Code_check_prompt_manual-PART1.txt` - Lines 1-500 (Introduction, Steps 1-2, Sections A-B-C-D partial)
- `prompts/Code_check_prompt_manual-PART2.txt` - Lines 501-1000 (continues D, covers E-F-G-H)
- `prompts/Code_check_prompt_manual-PART3.txt` - Lines 1001-1500 (continues H, covers I-J-K-L-M-O partial)
- `prompts/Code_check_prompt_manual-PART4.txt` - Lines 1501-1595 (continues O, covers P, Steps 3-4, Protocols)

**SEQUENTIAL CHECKING ENFORCED**: Process parts in order (PART1 → PART2 → PART3 → PART4). Do not skip or merge parts.
